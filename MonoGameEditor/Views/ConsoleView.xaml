<UserControl x:Class="MonoGameEditor.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ConsoleTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D30"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="4,0">
                <Button Content="Clear" Click="ClearButton_Click" 
                        Background="#3E3E42" Foreground="White" BorderThickness="0"
                        Margin="4,2" Padding="8,4"/>
                <Button Content="📋 Copy All" Click="CopyButton_Click" 
                        Background="#3E3E42" Foreground="White" BorderThickness="0"
                        Margin="4,2" Padding="8,4"
                        ToolTip="Copiar todos os logs para clipboard"/>
            </StackPanel>
        </Border>

        <!-- Tabs -->
        <Border Grid.Row="1" Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="8,0">
                <RadioButton GroupName="ConsoleTabs"
                             Style="{StaticResource ConsoleTabStyle}"
                             IsChecked="True"
                             Tag="0"
                             Checked="Tab_Checked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="☰" Margin="0,0,4,0" FontSize="12"/>
                        <TextBlock Text="All" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding AllCount, StringFormat='({0})'}" FontSize="11" Opacity="0.7"/>
                    </StackPanel>
                </RadioButton>
                
                <RadioButton GroupName="ConsoleTabs"
                             Style="{StaticResource ConsoleTabStyle}"
                             Tag="1"
                             Checked="Tab_Checked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ℹ" Margin="0,0,4,0" FontSize="12"/>
                        <TextBlock Text="Info" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding InfoCount, StringFormat='({0})'}" FontSize="11" Opacity="0.7"/>
                    </StackPanel>
                </RadioButton>
                
                <RadioButton GroupName="ConsoleTabs"
                             Style="{StaticResource ConsoleTabStyle}"
                             Tag="2"
                             Checked="Tab_Checked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠" Margin="0,0,4,0" FontSize="12" Foreground="#FFA500"/>
                        <TextBlock Text="Warning" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding WarningCount, StringFormat='({0})'}" FontSize="11" Opacity="0.7"/>
                    </StackPanel>
                </RadioButton>
                
                <RadioButton GroupName="ConsoleTabs"
                             Style="{StaticResource ConsoleTabStyle}"
                             Tag="3"
                             Checked="Tab_Checked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✗" Margin="0,0,4,0" FontSize="12" Foreground="#FF4444"/>
                        <TextBlock Text="Error" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding ErrorCount, StringFormat='({0})'}" FontSize="11" Opacity="0.7"/>
                    </StackPanel>
                </RadioButton>
            </StackPanel>
        </Border>

        <!-- Console Output -->
        <ListBox Grid.Row="2" x:Name="LogListBox"
                 ItemsSource="{Binding DisplayedMessages}"
                 Background="#1E1E1E"
                 Foreground="#CCCCCC"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Icon}" 
                                   Foreground="{Binding Color}"
                                   Margin="0,0,8,0"
                                   FontSize="14"/>
                        <TextBlock Text="{Binding FormattedMessage}" 
                                   Foreground="{Binding Color}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="4,2"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2D2D30"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>
